# PrepRecipe for metaHIV_phy data

A Recipe created for a metaHIV_phy object uning "Riskgroup2" as a
`var_info` and "Genus" as a `tax_info`. Also includes `step_deseq` and
`step_maaslin`.

## Usage

``` r
data("test_prep_rec")
```

## Format

A PrepRecipe object.
