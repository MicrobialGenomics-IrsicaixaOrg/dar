% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe_qc.R
\name{recipe_qc}
\alias{recipe_qc}
\title{Recipe quality-control metrics}
\usage{
recipe_qc(rec, group_by = NULL)
}
\arguments{
\item{rec}{A \link[=Recipe-class]{Recipe} or \link[=PrepRecipe-class]{PrepRecipe} object.}

\item{group_by}{\code{NULL} or the name of one sample metadata column used to
stratify the QC summary.}
}
\value{
A tibble with grouping identifiers and QC metrics.
}
\description{
\code{recipe_qc()} summarizes sparsity and sequencing-depth metrics from the count
matrix stored in a \link[=Recipe-class]{Recipe} or \link[=PrepRecipe-class]{PrepRecipe} object. The summary
is independent of the centralized statistical model and can therefore be
used before model configuration and after recipe preprocessing.
}
\details{
With \code{group_by = NULL}, the result contains one overall row. When a sample
metadata column is supplied, the overall row is followed by one row for each
observed group. The \code{group_by} and \code{group} columns are \code{NA} in the overall
row, so a real group named \code{"all"} remains unambiguous.

The returned metrics are:
\itemize{
\item \code{n}: number of values in the count matrix.
\item \code{n_zero}: number of zero values.
\item \code{pct_zero}: percentage of zero values.
\item \code{pct_all_zero}: percentage of taxa absent from every sample.
\item \code{pct_singletons}: percentage of taxa detected in exactly one sample.
\item \code{pct_doubletons}: percentage of taxa detected in exactly two samples.
\item \code{n_samples}: number of samples.
\item \code{lib_size_min}: minimum sample library size.
\item \code{lib_size_max}: maximum sample library size.
\item \code{count_mean}: mean of the per-sample mean counts.
\item \code{count_max}: mean of the per-sample maximum counts.
}
}
\examples{
data(metaHIV_phy)

rec <- recipe(metaHIV_phy)

## Overall metrics do not require a statistical model
recipe_qc(rec)

## Add explicit sample-level stratification when useful
recipe_qc(rec, group_by = "RiskGroup2")
}
