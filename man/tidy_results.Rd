% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result-contract.R
\name{tidy_results}
\alias{tidy_results}
\title{Collect differential-abundance results in a tidy contract}
\usage{
tidy_results(rec, steps = steps_ids(rec, "da"), significant_only = FALSE)
}
\arguments{
\item{rec}{A \link[=PrepRecipe-class]{PrepRecipe} object.}

\item{steps}{Character vector of executed differential-abundance step IDs to
collect. By default, all executed DA steps are included.}

\item{significant_only}{If \code{TRUE}, retain only rows classified as
significant by their originating step.}
}
\value{
A deterministic tibble with one row per taxon, contrast and
configured step. It contains taxon identifiers (\code{taxa_id}, \code{taxa}),
contrast metadata (\code{contrast_id}, \code{comparison}, \code{contrast_type}, \code{var},
\code{numerator}, \code{denominator}, \code{at_var}, \code{at_level}), statistics
(\code{effect_size}, \code{effect_metric}, \code{p_value}, \code{adj_p_value}, \code{significant}),
the stable engine in \code{method}, and the configured instance in \code{step_id}.
}
\description{
\code{tidy_results()} combines the results of executed differential-abundance
steps without changing the engine-specific tables stored in \code{rec@results}.
Effect sizes are labelled with their engine-specific metric and must not be
compared directly across incompatible metrics.
}
\examples{
data(test_prep_rec)
results <- tidy_results(test_prep_rec)
results

tidy_results(test_prep_rec, significant_only = TRUE)
}
