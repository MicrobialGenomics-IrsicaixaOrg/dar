% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialization.R
\name{import_steps}
\alias{import_steps}
\title{Import a recipe configuration from versioned JSON}
\usage{
import_steps(
  rec,
  file,
  prepare = FALSE,
  parallel = TRUE,
  workers = future::availableCores(constraints = "connections-16")
)
}
\arguments{
\item{rec}{A \code{\link[=Recipe]{Recipe()}} or \code{\link[=PrepRecipe]{PrepRecipe()}} object.}

\item{file}{Input JSON path.}

\item{prepare}{Whether an unprepared recipe may run \code{\link[=prep]{prep()}} when the file
contains bake configurations. Defaults to \code{FALSE}.}

\item{parallel}{Passed to \code{\link[=prep]{prep()}} only when \code{prepare = TRUE}.}

\item{workers}{Passed to \code{\link[=prep]{prep()}} only when \code{prepare = TRUE}.}
}
\value{
The configured \code{Recipe} or \code{PrepRecipe}.
}
\description{
Recipe files are treated as untrusted input. The complete document is
decoded and validated before supported constructors are applied. Importing
bake configurations into an unprepared recipe never runs analysis unless
\code{prepare = TRUE} is supplied explicitly.
}
\examples{
data(metaHIV_phy)
configured <- recipe(metaHIV_phy) |>
  add_model(~ RiskGroup2, targets = "RiskGroup2", tax_level = "Species") |>
  step_filter_by_prevalence(threshold = 0.1)
path <- tempfile(fileext = ".json")
export_steps(configured, path)
restored <- import_steps(recipe(metaHIV_phy), path)
}
