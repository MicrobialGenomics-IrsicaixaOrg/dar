# Function step_lefse() @ L83

    Code
      expect_s4_class(suppressWarnings(prep(test, parallel = FALSE)), "PrepRecipe")
    Message
      ! Rarefaction is enabled (with set seed).
      i This process is not without controversy. Use `rarefaction_help()` to get more info.
      The outcome variable is specified as 'RiskGroup2' and the reference category is 'hts'.
       See `?factor` or `?relevel` to change the reference category.
      The outcome variable is specified as 'RiskGroup2' and the reference category is 'hts'.
       See `?factor` or `?relevel` to change the reference category.
      The outcome variable is specified as 'RiskGroup2' and the reference category is 'msm'.
       See `?factor` or `?relevel` to change the reference category.
      The outcome variable is specified as 'RiskGroup2' and the reference category is 'hts'.
       See `?factor` or `?relevel` to change the reference category.
      The outcome variable is specified as 'RiskGroup2' and the reference category is 'hts'.
       See `?factor` or `?relevel` to change the reference category.
      The outcome variable is specified as 'RiskGroup2' and the reference category is 'msm'.
       See `?factor` or `?relevel` to change the reference category.

